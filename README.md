# MERN Stack Project

This is a modern web application built using the MERN stack (MongoDB, Express, React, Node.js). 
The project structure is divided into two main parts: the `frontend` and the `backend`.

## Project Structure

- `frontend/`: Contains the React application created with Vite.
- `backend/`: Contains the Node.js and Express API.

## Prerequisites

- Node.js (v18 or higher recommended)
- MongoDB (local instance or MongoDB Atlas)

## Getting Started

### Backend Setup

1. Navigate to the backend directory:
   ```bash
   cd backend
   ```
2. Install the required dependencies (Express, Mongoose, CORS, dotenv):
   ```bash
   npm install
   ```
3. Create a `.env` file and configure your environment variables (e.g., `MONGO_URI`, `PORT`).
4. Start the backend server:
   ```bash
   npm start
   ```

### Frontend Setup

1. Navigate to the frontend directory:
   ```bash
   cd frontend
   ```
2. Install the required dependencies:
   ```bash
   npm install
   ```
3. Start the development server:
   ```bash
   npm run dev
   ```

## Development

Currently, this acts as the initialization template for our app. 
Going forward, I will update this README with specific instructions, API documentation, and configuration details as the project evolves.
